const doesPathExist = require('./does-path-exists'),
	formatDate = require('./format-date'),
	validateInput = require('./validate-input')
module.exports = {
	validateInput,
	doesPathExist,
	formatDate
}
